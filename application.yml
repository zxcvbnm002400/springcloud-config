spring:
  profiles:
    active:
    - dev
    
---
# 开发环境
spring:
  profiles: dev
  application:
    name: config-server-dev

---
# 测试环境
spring:
  profiles: test
  application:
    name: config-server-test
